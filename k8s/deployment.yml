apiVersion: apps/v1
kind: Deployment
metadata: 
  name: basic-ci-cd
spec: 
  replicas: 2

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1

  selector:
    matchLables:
      app: basic-ci-cd
  
  template:
    metadata:
      labels:
        app: basic-ci-cd

    spec:
      containers:
      - name: basic-ci-cd
        image: phutawan1906/myapp:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
